{
  "name": "@diamond/abi",
  "version": "1.1.0",
  "description": "Canonical IDL + TypeScript types for the Diamond Pools Solana program. Source of truth \u2014 never edit dist/ by hand; run `scripts/sync.sh` after a contract rebuild.",
  "license": "MIT",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist",
    "src"
  ],
  "scripts": {
    "build": "npm run clean && tsc -p tsconfig.json",
    "sync": "bash scripts/sync.sh",
    "clean": "rm -rf dist",
    "prepare": "npm run build"
  },
  "devDependencies": {
    "typescript": "^5.7.3"
  }
}
