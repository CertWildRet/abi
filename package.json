{
  "name": "@cwr/abi",
  "version": "0.1.0",
  "description": "Canonical IDL + TypeScript types for CWR Solana programs. Source of truth — never edit dist/ by hand; run `scripts/sync.sh` after a contract rebuild.",
  "license": "MIT",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist",
    "src"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "sync": "bash scripts/sync.sh",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "typescript": "^5.7.3"
  }
}
